# Security Notes

- Never commit secrets. Use `.env` or your deployment secret manager.
- Default repo examples target local or self-hosted environments.
- Require explicit confirmation before destructive or paid actions.
- Treat `local_trusted` mode as development-only.
