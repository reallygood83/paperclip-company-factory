# Core
PAPERCLIP_BASE_URL=http://127.0.0.1:3100
PAPERCLIP_API_PREFIX=/api
PAPERCLIP_API_KEY=
PAPERCLIP_MODE=local_trusted

# Hermes
HERMES_ENABLED=true
HERMES_PLATFORM=telegram
HERMES_BRIDGE_MODE=manual

# Factory defaults
DEFAULT_TEMPLATE=research-company
DEFAULT_DEPLOY_TARGET=local
DEFAULT_VISIBILITY=private
DEFAULT_PROVIDER_PROFILE=openai-codex

# GitHub / CI
GITHUB_REPOSITORY=
GITHUB_TOKEN=
